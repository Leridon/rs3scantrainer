import {ActiveLayer} from "../../../lib/gamemap/activeLayer";
import {GameMap} from "../../../lib/gamemap/GameMap";

export default class ShortcutUtilLayer extends ActiveLayer {
    constructor() {
        super();
    }

    override activate(map: GameMap) {
        super.activate(map);


    }
}