import {ActiveLayer} from "../activeLayer";
import {GameMapControl} from "../map";

export default class ShortcutUtilLayer extends ActiveLayer {
    constructor() {
        super();
    }

    override activate(map: GameMapControl) {
        super.activate(map);


    }
}